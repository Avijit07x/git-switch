// Repository-related Tauri commands. Folder selection happens in the frontend
// through the dialog plugin; this module is a placeholder for future
// repository-level (non-Git) operations such as persisted metadata lookups.
// Kept separate from `git::commands` for single-responsibility.
