node_modules
dist
dist-ssr
*.local
.DS_Store

# Tauri
src-tauri/target
# NOTE: Cargo.lock IS committed for this app. Tauri ships a binary, so we
# pin dependency versions for reproducible CI / release builds. Removing
# this exclusion was the fix for the `cargo check --locked` failure in CI.

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# CI scratch
RELEASE_NOTES.md
